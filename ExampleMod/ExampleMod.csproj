
<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\tModLoader.targets" />
  <PropertyGroup>
    <AssemblyName>ExampleMod</AssemblyName>
    <LangVersion>latest</LangVersion>
    <!-- Keep solution builds deterministic; run mod packaging only when explicitly requested with /p:BuildMod=true -->
    <BuildMod>false</BuildMod>
  </PropertyGroup>
  <ItemGroup>
    <Compile Remove="Old\**" />
    <EmbeddedResource Remove="Old\**" />
    <None Remove="Old\**" />
  </ItemGroup>
</Project>
